
@import '../../variables.pcss';


.doc > section.footer {
	flex: 2;
	display: flex;
	justify-content: flex-start;
	align-items: flex-end;
	margin: 30px;
	color: color(#000 lightness(40%));
	font-size: 0.85em;
	font-weight: var(--font-weight-light);

	a {
		margin-left: 0.3em;
	}
}
